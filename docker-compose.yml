version: '3.6'

services:
  smart-brain-api:
    container_name: backend
    image: node:8.16.01  #(but this is built from Dockerfile immage)
    build: ./
    command: npm start
    working_dir: /usr/src/smart-brain-api
    ports:
      - "3000:3000"
